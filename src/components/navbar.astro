---
import { MobileNav } from "~/components/mobile-nav"
import { ModeToggle } from "~/components/mode-toggle"
import { navItems } from "~/lib/data"
---

<header
  class="sticky top-0 z-50 w-full bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60"
>
  <div class="container m-auto flex h-14 items-center justify-between px-4">
    <MobileNav client:load />
    <div class="hidden text-xl font-bold md:inline-block">SK</div>
    <div class="flex items-center gap-6">
      {
        navItems.map((item) => (
          <a
            href={item.href}
            title={item.name}
            class="hidden font-medium text-muted-foreground transition-colors hover:text-foreground md:inline-block"
          >
            {item.name}
          </a>
        ))
      }
      <ModeToggle client:only />
    </div>
  </div>
</header>
